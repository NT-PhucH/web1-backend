{
  "name": "web1-backend",
  "version": "1.0.0",
  "description": "Backend API for web1 Schedule Scraper",
  "main": "server.js",
  "scripts": {
    "build": "npm install",
    "start": "node server.js"
  },
  "config": {
    "postinstall": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true",
    "puppeteer_skip_chromium_download": "true"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@sparticuz/chromium": "^141.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer-core": "^24.30.0"
  }
}
